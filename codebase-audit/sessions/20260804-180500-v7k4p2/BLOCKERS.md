# Outstanding Blockers — Independent Validation

Internal code defects are separated from external environment blockers. No blocker is marked resolved without direct evidence.

| Blocker | Current Status | Evidence | Required Environment | Exact Next Validation | Release Impact |
| ------- | -------------- | -------- | -------------------- | --------------------- | -------------- |
| **Code-signing certificate / Authenticode pin population** | STILL OPEN (confirmed) | `src/setup/include/ffsetup/PinnedSignatures.h:16-17`: `constexpr Thumbprint kExpectedEngineSignatureThumbprint{};` / `kExpectedIndexSvcSignatureThumbprint{};` — both all-zero placeholders. `src/setup/src/AuthenticodeVerification.cpp:77-78`: `return false; // fail closed: pin not yet configured`. | A provisioned code-signing certificate + release signing pipeline. | Provision a real Authenticode cert; populate the two thumbprints; run `fftest` privilege-diagnostics probe + protocol fuzz tests to confirm invalid signatures are rejected; run signed-install + service validation under elevation. | **Blocks privileged-path production readiness only.** Degraded-mode path is unaffected. AGENTS.md explicitly states degraded mode is the active/safe path today. Does NOT block degraded-mode release. |
| **Non-elevated runtime validation (privileged path)** | STILL OPEN (confirmed) | No service install/start/run performed this session (no UAC). `verify/verify.ps1` install/service verbs need `-Elevate`. AGENTS.md: "Do not assume the privileged scan path is production-validated end-to-end." | An authorized elevated Windows host. | `pwsh ./verify/verify.ps1 install -Elevate` then `run -Elevate`/`diagnose -Elevate`; confirm `ConnectionState::Active` transition and privileged scan (not degraded). | **Blocks privileged-path readiness only.** Degraded mode remains valid. |
| **Clean-host matrix rerun** | STILL OPEN (confirmed) | `openspec/changes/resolve-raw-volume-privilege-insufficiency/evidence/matrix-execution-and-selection.md` records: clean disposable image + two secondary hosts (Win10 22H2 / Server 2022) were not available; the negative result (only Administrators/SYSTEM open the raw volume) is recorded as robust-to-host-state but not matrix-complete. | A clean disposable image + the two secondary hosts. | Re-run `Run-PrivilegeCandidateMatrix.ps1` on a clean host + secondary OSes; attach per-row JSON + summary. | Narrows confidence in the LocalSystem decision but does not block degraded mode. The selected model (LocalSystem) is already implemented. |
| **Windows UI validation runs (visual)** | STILL OPEN (confirmed) | Multiple OpenSpec tasks tagged `SKIPPED(treemap-drawn-on-d2d-surface)` / `column-items-not-exposed-to-uia` (no UIA provider for D2D surfaces); UI visual matrix (Light/Dark × 100%/150% DPI × animations on/off) is a documented manual pass (`modernize-ui-appearance` task 6.3). | An interactive Windows desktop session. | Manual smoke matrix: column nav, search, treemap, dual-pane, settings, theme cross-fade, idle-CPU sanity; plus the deferred UI items M1/M3/M5(display)/L9(display)/L11. | **Blocks visual approval of UI changes (M4/L7/L10/L1 + deferred items) only.** Does not block build/test correctness. The headless UIA-driver + animation tests pass. |

## Internal code defects (not external blockers)

| Defect | Severity | Status | Evidence |
| ------ | -------- | ------ | -------- |
| Stale "virtual service account… SeBackupPrivilege only (never LocalSystem/admin)" in `CLAUDE.md:41-42` and `CODE_INDEX.md:148-149` | Low (docs consistency) | Open | `search_codebase` confirmed. AGENTS.md and design.md were corrected in HEAD `216d931` but CLAUDE.md/CODE_INDEX.md were not. Directly contradicts the corrected authoritative docs. |
| Committed binary `dist/FastFiles.exe` (862208 bytes) and 819-line `Audit.txt` prompt template in HEAD `216d931` | Low (hygiene) | Open | `git show --stat HEAD`. Binaries and prompt templates generally should not be committed to source control. Not part of any supplied claim. |
| "26/26" / "test #26" / "previously 25" misreporting | Low (reporting accuracy) | Resolved-by-this-validation | `ctest -N` + full run: 27/27, Test #27. The supplied addendum's numbers are inaccurate. |

No elevated, destructive, signing, installation, or clean-host operations were performed (not authorized/suitable for this session).
