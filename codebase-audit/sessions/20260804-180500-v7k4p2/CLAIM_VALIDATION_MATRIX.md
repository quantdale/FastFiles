# Claim Validation Matrix — Independent Validation

Every supplied claim was treated as unverified until independently confirmed against the actual code and executed results. Confidence: High = direct code inspection + executed evidence; Medium = static inspection only; Low = indirect inference.

| Claim ID | Supplied Claim | Evidence Reviewed | Commands Run | Result | Confidence | Notes |
| -------- | -------------- | ----------------- | ------------ | ------ | ---------- | ----- |
| C-Item1 | `ffintake_gate_ps_tests` added to `tests/uia-driver/CMakeLists.txt` | `tests/uia-driver/CMakeLists.txt` (16–21) | `ctest -N`, `ctest -R ffintake_gate_ps_tests -V` | **Confirmed** | High | Registration: `add_test(NAME ffintake_gate_ps_tests COMMAND pwsh -File run-intake-gate-tests.ps1)`, TIMEOUT 120, gated on `find_program(pwsh)`. |
| C-Item1-num | It is test #26 | `ctest -N` output | `ctest -N` | **Contradicted** | High | It is **Test #27**. Total is **27**, not 26. `ffuia_driver_ps_tests` is #26 (pre-existed before HEAD). |
| C-Item1-pass | It passes | direct run output | `ctest -R ffintake_gate_ps_tests -V` | **Confirmed** | High | 10/10 assertions "ok"; exit 0; 1.12s. |
| C-Item1-script | Script path resolves; quoting handles spaces; correct pwsh | CMakeLists + ctest -V | `ctest -R ... -V` | **Confirmed** | High | Command uses full quoted path through repo dir with spaces; `-NoProfile -ExecutionPolicy Bypass`. |
| C-Item1-fp | Test cannot falsely pass | `run-intake-gate-tests.ps1` | (static) | **Confirmed** | Medium | `$ErrorActionPreference='Stop'`; `Check` increments failures; `exit 1` if >0. Assertions check `(-not $outcome.ok)`, classification, summary content, `external` — all can fail. No silent skip. |
| C-Item1-fresh | Included in fresh CMake config | wiped `build/debug`, reconfigured | `Remove-Item build\debug; cmake --preset debug; ctest -N` | **Confirmed** | High | Test #27 appears after clean wipe+reconfigure. |
| C-Item2 | `storage-analysis/tasks.md` refs updated `TreemapView::Squarify`/`HitTest` → `ffui::Squarify`/`HitTest` in `TreemapLayout.h` | diff + repo search | `git diff`, `search_codebase` | **Confirmed** | High | Exact replacement in diff. Stale refs remain only inside the audit merged-input file, not in source. Free functions in `ffui` namespace. |
| C-Item3 | `PrivilegeVerification.h` comment rewritten: virtual account→LocalSystem; stubbed→implemented | diff + impl + design + registration | `git diff`, `search_codebase` | **Confirmed** (file) | High | New comment accurate: `ServiceRegistration.h` `LocalSystem // constrained-broker`; `ServiceConnection.cpp` dispatches real scanner; matrix evidence shows LocalSystem opens volume. |
| C-Item3-stale | No remaining "virtual account/stubbed" claims | repo-wide search | `search_codebase` | **Contradicted** | High | `CLAUDE.md:41-42` STILL says "virtual service account… SeBackupPrivilege only (never LocalSystem/admin)". `CODE_INDEX.md:148-149` STILL says "virtual account (NT SERVICE\FastFilesIndexSvc)". Remaining contradiction. |
| C-Item4 | `design.md` superseded marker + D5 correction | diff | `git diff` | **Confirmed** | High | Non-Goals gets superseded marker citing `index-storage-and-scanning`; D5 "stubbed at the time… implemented later"; D4 rewritten to LocalSystem constrained-broker. |
| C-Item4-impl | `index-storage-and-scanning` actually implemented scanner | `ServiceConnection.cpp` etc. | `search_codebase` | **Confirmed** | High | Real scan dispatch + batch streaming; no `NotYetImplemented`. |
| C-Item4-stale | No other stale "stubbed MFT/USN" claims | repo-wide search | `search_codebase` | **Partially Confirmed** | High | `AGENTS.md` corrected this commit. But `CLAUDE.md:42` still implies old model. design.md/AGENTS.md clean. |
| C-M4 | `WindowShell` propagates engine status via `storageAnalysis_.SetEngineActive(...)` | diff + call sites | `git diff`, `search_codebase` | **Confirmed** | High | `WM_APP_ENGINE_STATUS` handler now calls `storageAnalysis_.SetEngineActive(engineActive_)` + `RequestRepaint()`. |
| C-defer-M1 | M1 (loading vs empty) not accidentally modified | diff | `git diff` | **Confirmed** | High | No diff to the loading/empty path beyond the empty-state message already present. Still requires visual validation. |
| C-defer-M3 | M3 (details-pane overlap) not accidentally modified | diff `WindowShell.cpp` details + `RenderDetails` (880-893) | `git diff`, `read_files` | **Confirmed still applicable (defect)** | High | `RenderDetails` = fixed 300px right-anchored floating card; ColumnView gets full viewport width (no reservation) → rightmost columns render under the card at narrow widths. Restyle changed visuals, not width reservation. |
| C-defer-M5 | M5 (dark-theme coverage) | `settings-and-appearance` task 11.2 | `search_codebase` | **Superseded/Resolved** | Medium | Task 11.2 (complete) states dark-theme coverage ALREADY fixed: details pane, sidebar, search edit, treemap now theme-aware. Likely already resolved — needs visual confirm. |
| C-defer-L5 | L5 (metric consolidation) | `ColumnView.h:143-145` | `search_codebase` | **Resolved (false positive)** | High | `kColumnWidth/kRowHeight/kBadgeHeight` now ALIAS `UiMetrics::` ("cannot diverge"). Addressed by `modernize-ui-appearance` task 1.2 — not still-open. |
| C-defer-L9 | L9 (% of Parent calculation) | `StorageAnalysis.cpp:57-60` (FormatPercent) + `681-691` (display case 4) + `524-530` (sort case 4) | `search_codebase`, `read_files` | **Defect found** | High | `FormatPercent` math is correct (`part/whole*100`, zero guard), BUT the `whole` passed is wrong: `sum of sibling *directories'* totalSizeBytes` (excludes files; not the parent's total). Mislabel; files show "—"; don't sum to 100% when parent has files. Sort key `totalSizeBytes*1000000ULL` can overflow uint64 for >~18 TB folders. |
| C-defer-L11 | L11 (sidebar highlight) | diff `NavigationSidebar.cpp` | `git diff` | **Confirmed deferred** | High | Sidebar restyled (task 5.2) but specific highlight not targeted. Still applicable; requires visual validation. |
| C-build | `cmake --build --preset debug` clean | clean rebuild | `cmake --build --preset debug` | **Confirmed** | High | Exit 0; 159/159 steps; all exes linked; ~20.6s. No warnings/errors in build.log. |
| C-W4WX | Strict `/W4 /WX` | `build.ninja:170` + `CMakeLists.txt` | `grep build.ninja` | **Confirmed** | High | `FLAGS=.../W4 /WX /permissive- /sdl /guard:cf /Zc:__cplusplus`; CMakeLists also `/DYNAMICBASE /NXCOMPAT`. No target disables WaE. |
| C-26tests | `ctest` 26/26; previously 25 | full suite run | `ctest --output-on-failure` | **Contradicted** | High | Actual: **27/27 passed (100%)**, ~22.1s. NOT 26/26. New test is #27. "Previously 25" inaccurate — `ffuia_driver_ps_tests` (#26) pre-existed; before intake-gate test there were 26. |
| C-newtest-reg | New test registered | `ctest -N` | `ctest -N` | **Confirmed** | High | `ffintake_gate_ps_tests` = Test #27 in fresh discovery. |
| C-codesign | Code-signing blocker outstanding | `PinnedSignatures.h:16-17` | `search_codebase` | **Confirmed (still open)** | High | Both thumbprints all-zero placeholders; `AuthenticodeVerification.cpp:77` fails closed. |
| C-nonelev | Non-elevated runtime validation outstanding | (no service install/run) | — | **Confirmed (still open)** | High | No elevated run this session. Privileged-path runtime unvalidated. |
| C-cleanhost | Clean-host matrix rerun outstanding | matrix evidence doc | — | **Confirmed (still open)** | Medium | Clean disposable image + secondary hosts unavailable; negative result recorded. |
| C-uivalid | Windows UI validation runs outstanding | task files (SKIPPED tags) | — | **Confirmed (still open)** | High | Multiple `SKIPPED(treemap-drawn-on-d2d-surface)`/`column-items-not-exposed-to-uia`; UI visual matrix is a manual pass. |
| C-wt | "No changes committed; working tree contains all modifications" | `git status` | `git status --porcelain=v2` | **Contradicted** | High | Working tree is **CLEAN** for tracked files; all claimed changes ARE committed in HEAD `216d931`. Only untracked file is the merged-input md. Addendum's working-tree claim is inaccurate. |

| C-M4-root | Fixes root cause, not one path | `WindowShell.cpp` handler | (static) | **Confirmed** | Medium | All status transitions route through the single `WM_APP_ENGINE_STATUS` handler; storage panel gets the same value as search/settings panels. |
| C-L7 | Storage context-menu relabeled "Move…"→"Cut"; command already `file.cut` | `StorageAnalysis.cpp:783,803-804` | `search_codebase` | **Confirmed** | High | `L"Cut"`; `case 4: invokeCommand_(L"file.cut", paths)`. No "Move…" anywhere. |
| C-L10 | Selected-but-unfocused rows use normal text brush | diff `ColumnView.cpp` | `git diff` | **Confirmed** | High | `isSelected && isFocusedColumn ? textOnAccentBrush_ : textBrush_`; unfocused uses `selectionSoftBrush_`. |
| C-L10-a11y | Accessibility improvement across themes | diff + HC branch | (static) | **Partially Confirmed** | Medium | HC path keeps system colors. Token text brushes for light/dark. No visual measurement performed. |
| C-L1 | Dead `SystemAnimationsEnabled` removed (decl + comments) | diff `WindowShell.cpp`/`.h` | `git diff`, `search_codebase` | **Confirmed** | High | Dead `WindowShell::SystemAnimationsEnabled() const` member removed. Live `ffui::SystemAnimationsEnabled()` (UiAnimation.h/cpp) REMAINS and gates `FloatAnimation::AnimateTo`. AGENTS.md documents the live one. |
| C-L1-a11y | No reduced-motion regression | UiAnimation impl + test | `search_codebase` | **Confirmed** | Medium | `AnimateTo` snaps when `!SystemAnimationsEnabled()`. `test_ui_style.cpp` exercises it. No HC/RDP regression. |
