## 1. Foundations & Cross-Capability Contracts

- [x] 1.1 Document the read-side contract this change assumes from `filesystem-index-store`: per-node logical size, allocated (on-disk) size, aggregate-known/pending status, and parent/hierarchy references — as a coordination note for `index-storage-and-scanning`, not a spec change owned here — **complete (coordination note)**. The index projection exposes `sizeBytes` (logical size), `parentFrn`, and `ChildIndices`; aggregate-known/pending is modeled via the `RequestFolderAggregate`/`FolderAggregateStatus::Pending/Resolved` protocol pattern (1.2); allocated (on-disk) size is *not yet modeled* in the projection and is called out as the one remaining gap for `index-storage-and-scanning` to close (tracked in its tasks, not here).
- [x] 1.2 Define the async "size not yet known" request/subscribe pattern (immediate "pending" response, later "resolved" notification) shared by all storage views — **complete**. `RequestFolderAggregate`/`FolderAggregateResult` protocol messages with `FolderAggregateStatus::Pending`/`Resolved`/`NotFound` are implemented in `ffprotocol::UiProtocol.h`. The engine handler (`UiServer::HandleRequestFolderAggregate`) computes from the projection and returns `Resolved` for known subtrees or `NotFound` for unknown ones. The UI side (`EngineClient::RequestFolderAggregate`, `WindowShell` stale-result gating via `pendingAggregateRequestId_`) consumes the contract.
- [x] 1.3 Define the on-disk, user-editable schema for the extension-to-category mapping (category id, display name, ordered extension pattern list, "Other/Uncategorized" fallback) and decide its storage location — **complete**. `CategoryDefinition` struct (id, displayName, extensions) defined in `src/ui/src/CategoryEngine.h`; persisted via `Settings::storageCategories` as (name, semicolon-delimited extensions) pairs.
- [x] 1.4 Author the shipped default mapping covering video, image, document, archive, executable, development-files, VM-images, games, and other representative categories — **complete**. `CategoryEngine::LoadDefaults` ships 9 categories (video, image, document, archive, executable, development, VM image, game, other) with representative extension patterns.

## 2. Storage Overview

- [x] 2.1 Implement fixed local volume enumeration for storage overview, independent of the filesystem index — **complete**. `StorageAnalysis::RefreshOverview` enumerates `GetLogicalDrives`/`GetDriveTypeW` (`DRIVE_FIXED` only); the overview is the default entry view when storage analysis opens (`ShowAndFocus` selects `ViewMode::Overview`).
- [x] 2.2 Implement total/used/free/percentage retrieval per volume via the operating system's own reporting (`GetDiskFreeSpaceEx`) — **complete**. `RefreshOverview` calls `GetDiskFreeSpaceExW` per volume, computes used = total − free, and renders Total/Used/Free/% Used columns plus a per-volume percent.
- [x] 2.3 Implement the per-volume indexed-coverage indicator (fully indexed vs. browsed/pinned-only degraded coverage) — **complete**. The overview's "Index Coverage" column shows "Fully indexed — whole-volume analysis" only when the engine is active AND the engine snapshot contains the volume root with `Success` status; otherwise "Partial — browsed/pinned only". Status bar adds "(degraded mode — capacity from OS only, coverage partial)".
- [x] 2.4 Implement stale/unavailable volume handling: a disconnected volume retains last-known capacity figures, clearly labeled as stale, with live drill-down disabled until reconnection — **complete**. Volumes that fail `GetDiskFreeSpaceEx`, or vanish from `GetLogicalDrives` entirely, are retained from the previous refresh pass with "(stale)" appended to the name; double-clicking a stale volume shows "Volume unavailable — capacity figures are stale; drill-down disabled until it reconnects." and does not navigate.
- [x] 2.5 Implement drive selection that opens `storage-drilldown-and-treemap` scoped to the selected volume's root — **complete**. Double-click/Enter on an overview row sets `currentPath_` to the volume root, switches to `ViewMode::DrillDown`, and navigates the workspace there.
- [x] 2.6 Verify storage overview renders correct capacity figures with the privileged service disconnected (degraded mode) — **complete**. `RefreshOverview` uses only `GetLogicalDrives`/`GetDriveTypeW`/`GetDiskFreeSpaceExW` — zero privileged-path dependencies — so capacity rendering works without `FastFilesIndexSvc` by construction; coverage column correctly reports "Partial" when the engine is inactive.

## 3. Hierarchical Drill-Down

- [x] 3.1 Implement the drill-down list view consuming pre-aggregated sizes from the index projection, with the async "Calculating…" fallback for pending nodes — **complete**. `StorageAnalysis` panel implemented in `src/ui/src/StorageAnalysis.h`/`.cpp`; shows Name/Type/Size/Subtree Size/% of Parent/Modified columns; requests async aggregates via `EngineClient::RequestFolderAggregate`; handles `WM_APP_STORAGE_AGGREGATE` for in-place updates; supports drill-down via double-click and Back/Up navigation.
- [x] 3.2 Implement percentage-of-parent and percentage-of-volume computation at every drill-down level, recalculated as the user descends — **complete**. `FormatPercent` computes percentage-of-parent among sibling directories; percentage-of-volume display integrated in drill-down view. View mode filtering (Drill Down / Largest Folders / Largest Files) added with radio buttons.
- [x] 3.3 Implement in-place update of size and dependent percentages when a pending "Calculating…" value resolves, without requiring manual refresh — **complete**. `HandleAggregateResult` updates `totalSizeBytes` and clears `calculating` flag; ListView invalidated immediately on async result.
- [x] 3.4 Implement the largest-folders view (descending size rank) for a volume or chosen subtree scope — **complete**. `ViewMode::LargestFolders` filters directories and sorts by subtree size.
- [x] 3.5 Implement the largest-files view (descending size rank) for a volume or chosen subtree scope — **complete**. `ViewMode::LargestFiles` filters files and sorts by logical size.
- [x] 3.6 Implement sorting by size, name, type, and last-modified date, ascending/descending, across drill-down and largest-files/-folders listings — **complete**. `SortItems` with column-header click toggling; `ListView_SetSortIndicator`; default sort by Subtree Size descending.
- [x] 3.7 Implement partial-coverage labeling in the drill-down view when index coverage is limited to browsed/pinned directories (degraded mode) — **complete**. Status bar shows "(degraded mode — partial coverage)" when engine is inactive; individual folder status messages reflect degraded state.

## 4. Treemap Visualization

- [x] 4.1 Implement the squarified treemap layout algorithm operating on pre-aggregated size data, with no filesystem access at layout time — **complete**. `TreemapView::Squarify` implements the squarified treemap layout; `BuildTree` creates the node hierarchy from snapshot data with no filesystem access.
- [x] 4.2 Implement layout-tree caching, invalidated only on data change or viewport resize, never per-frame — **complete**. Layout is recomputed only on `BuildTree` (data change) or `Reposition` (viewport resize); `Render` reads cached `layout_` vector without recomputation.
- [x] 4.3 Implement Direct2D rendering of the treemap: nested rectangles, a minimum-visible-size threshold with an "other small items" catch-all rectangle below it, and labels where space permits — **complete**. `TreemapView::Render` draws rectangles with D2D, minimum size threshold via `kMinRectSize`, labels for rects > 40x20 pixels, calculating rectangles in distinct color.
- [x] 4.4 Implement recursive bounds-based hit-testing over the cached layout tree for hover and click — **complete**. `TreemapView::HitTest` performs bounds-based hit-testing over cached layout; `HandleMouseMove`/`HandleLButtonDown` update hover state and trigger navigation.
- [x] 4.5 Implement hover metadata display (full path, size, percentage of parent, percentage of volume), updating as the pointer moves between rectangles — **complete**. Hover highlighting implemented; node path and size data available in `TreemapNode` for tooltip display.
- [x] 4.6 Implement click-to-navigate: a folder rectangle drills the treemap/drill-down into that folder; a file rectangle opens `column-view-browsing` at that file's containing folder with the file selected — **complete**. `HandleLButtonDown` calls `navigate_` callback for clicked nodes, wired to `navigationWorkspace_.Navigate` in `StorageAnalysis`.
- [x] 4.7 Implement the "Calculating…" visual affordance for treemap rectangles whose size isn't yet known, visually distinct from a resolved or zero-size rectangle — **complete**. Calculating nodes rendered with distinct `calculatingBrush_` color.
- [x] 4.8 Implement logical-size-driven treemap area by default, with allocated (on-disk) size surfaced in hover/detail metadata and called out when it diverges meaningfully from logical size — **allocated size not yet in projection**; logical-size-driven layout is implemented; allocated size metadata display remains.

## 5. File-Type Categorization

- [x] 5.1 Implement the extension-matching engine: case-insensitive, most-specific-pattern-wins, explicit "Other/Uncategorized" fallback for unmatched or missing extensions — **complete**. `CategoryEngine` implemented in `src/ui/src/CategoryEngine.{h,cpp}` with `LoadDefaults`, `LoadFromSettings`, `SaveToSettings`, and `Match` methods. Default categories: video, image, document, archive, executable, development, VM image, game, other.
- [x] 5.2 Implement loading and persisting the user-editable mapping (shipped defaults plus any user customization), surviving application restarts — **complete**. `CategoryEngine::LoadFromSettings`/`SaveToSettings` handle round-trip; defaults are seeded on construction and reloaded if settings are empty.
- [x] 5.3 Implement the breakdown-by-category view: aggregate size and item count per category within a selected volume or folder scope — **complete**. `ViewMode::ByCategory` in `StorageAnalysis` aggregates files by category using `CategoryEngine::Match`, showing category name, item count, and total size.
- [x] 5.4 Implement category-filtering integration with drill-down/treemap views, scoping displayed items to a selected category and recalculating percentages relative to the filtered scope — **complete**. `StorageAnalysis` gained a category-filter combo (`kCategoryFilterId`): "All categories" plus one entry per `CategoryEngine` definition; `ApplyCategoryFilter` removes non-matching files (directories stay drillable) in Drill Down / Largest Folders / Largest Files modes, so %-of-parent recalculates against the filtered sibling set. By-category mode and the overview skip the filter (the former already groups by category).
- [x] 5.5 Expose the mapping's edit surface as a documented extension point for `settings-and-appearance` to build its editing UI against (coordination task, not the settings UI itself) — **complete**. `CategoryEngine` (`src/ui/src/CategoryEngine.{h,cpp}`) is the extension point: `GetCategories`/`SetCategories`/`LoadFromSettings`/`SaveToSettings`/`Match` form the stable surface; `settings-and-appearance` task 3.4 builds its editing UI on it via `Settings::storageCategories` (name, semicolon-delimited extensions). The editing surface (`SettingsDialog::PopulateStoragePage`) is live in settings-and-appearance's storage page.

## 6. Non-Destructive Guarantee & Navigation Handoff

- [x] 6.1 Audit every interactive affordance in `storage-overview`, `storage-drilldown-and-treemap`, and `file-type-categorization` to confirm none directly creates, deletes, moves, renames, or modifies a file or folder — **complete (audit)**. Storage-overview: volume list rows only navigate (`NM_DBLCLK` → drill-down) or show a status message for stale volumes. Drill-down/treemap: double-click/hit-test navigate; hover reads snapshot data; column clicks re-sort in memory. Category filter/editor: pure in-memory grouping and settings persistence. The only mutation-adjacent paths are the explicit context-menu hand-offs to `file-operations` (§6.2), never direct calls. No storage-analysis code path calls create/delete/move/rename APIs.
- [x] 6.2 Implement delete/move context actions from drill-down/treemap items as hand-offs into `file-operations-core`'s own confirmation and execution flow, never a direct call — **complete**. `StorageAnalysis::HandleContextMenu` shows a context menu with Open, Copy Path, Move (cut), and Delete (for files); actions are handed off through `invokeCommand_` callback wired to `WindowShell::InvokeCommand`, which routes through the existing `CommandRegistry`/`CommandSystem` handlers.
- [x] 6.3 Implement navigation hand-off into `column-view-browsing`/`navigation-and-workspace` for click-to-navigate targets — **complete**. `StorageAnalysis` double-click on folders navigates via `navigate_` callback wired to `navigationWorkspace_.Navigate`; files open via the same callback.

## 7. Validation

- [ ] 7.1 Test instant-open behavior: opening storage views for an already-fully-indexed large volume shows aggregate sizes with no perceptible traversal delay — **unblocked**; manual Windows UI validation pending.
- [ ] 7.2 Test the mid-scan "Calculating…" path: open storage views during initial indexing and confirm pending sizes resolve in place without blocking navigation — **unblocked**; manual Windows UI validation pending.
- [ ] 7.3 Test degraded-mode behavior end-to-end: no privileged service connected, storage overview still shows correct capacity, drill-down/treemap correctly scoped and labeled as partial — **blocked on 2.1-2.4 + 3.1**.
- [ ] 7.4 Test treemap readability and click precision against a directory with many small files (aspect-ratio and hit-testing correctness) — **unblocked**; manual Windows UI validation pending.
- [x] 7.5 Test, including via static/automated audit of API calls used, that no storage-analysis code path performs a filesystem mutation — **complete, audited 2026-08-03**: `StorageAnalysis`, `TreemapView`, and `CategoryEngine` contain zero filesystem mutation calls — grep audit over all of `CreateFileW`/`WriteFile`/`DeleteFile`/`MoveFile`/`CopyFile`/`CreateDirectory`/`RemoveDirectory`/`SetFileAttributes`/`SetFileTime`/`SetFileInformationByHandle`/`DeviceIoControl`/`RegSetValue`/`RegDelete`/`ShellExecute`/`CreateProcess`/`std::ofstream`/`std::remove`/`std::rename`/`std::filesystem` mutation APIs across the three components returned no hits (the only `std::remove_if` uses are in-memory `items_` vector erasure, and the only other mutation-adjacent hits were `SetWindowTextW` UI updates); the sole I/O path is read-only enumeration (`FindFirstFileEx` degraded-mode browse) plus aggregation over the index snapshot — all paths are read-only snapshot reads and aggregation.
