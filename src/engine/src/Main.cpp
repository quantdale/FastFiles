// FastFilesEngine: the unprivileged, per-logon-session index owner.
//
// This is the buildable skeleton for the process (task 1.2). The
// privileged-connection state machine, degraded-mode enumeration, and
// snapshot publication (tasks 4.1-4.10) land in a follow-up pass on this
// same change -- see openspec/changes/establish-architecture-foundation.
int wmain() {
    return 0;
}
