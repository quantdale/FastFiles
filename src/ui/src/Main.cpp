// FastFiles: the UI shell (Column View browsing).
//
// This is the buildable skeleton for the process (task 1.2). The
// Direct2D/DirectComposition window shell and multi-column navigation
// (tasks 5.1-5.10) land in a follow-up pass on this same change -- see
// openspec/changes/establish-architecture-foundation.
#include <windows.h>

int WINAPI wWinMain(HINSTANCE, HINSTANCE, LPWSTR, int) {
    return 0;
}
